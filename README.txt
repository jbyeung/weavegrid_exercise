# HOW TO RUN APP
This app can be run by initiating Docker image via the bash shell script.
> sudo bash start.sh

This will start the Docker container.  Run the container to start the Python API.

Then you can run the unit tests or hit the API with REST API calls.
> 








-------------------
TODO


unit test script that shows testing strategy


package to git and send URL
readme and how to run. how long it took to do exercise

test
- python api
- docker container / shell script working